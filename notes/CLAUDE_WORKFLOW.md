# Working with Claude on `aat`

This project gets ongoing help from Claude (Anthropic's assistant) across many separate sessions. Sessions don't share memory with each other except through whatever's written down -- conversation summaries carried forward by the tool are lossy and don't survive indefinitely, but this repo does. This file is where that continuity actually lives: how a session can reach this repo, what it delivers and how, and the standing constraints that apply regardless of which mode a given session is in. Read it at the start of any session picking this project back up, and add to it (see "Session log" below) at the end of any session that did something worth a future session -- or Neel -- knowing about.


## Local checkouts across machines

Neel works on this repo from more than one machine, each with its own independent local clone -- these are NOT synced automatically, so keep them in step with the usual `git pull`/`git push`, and don't assume one checkout's state (branch, working tree, uncommitted changes) matches another's without checking `git status`/`git log` fresh.

- **`candidemini-local`** -- `~/Desktop/syntax-cowork-repos/aat`. Cowork-linked as of 2026-09-21, so a device-linked Claude session can read and write here directly (see "Device-linked" below).
- **`pangloss-m3-504-local`** (Neel's other laptop) -- `~/Desktop/syntax-cowork-repos/aat` (same relative path, for consistency). Cowork-linked as of 2026-09-21 too, so a device-linked Claude session can read and write here directly as well. This is a separate, independent clone from `candidemini-local`'s -- not synced automatically, so keep both in step by hand with `git pull`/`git push` and don't assume one checkout's state matches the other's without checking fresh.

This repo previously also had a clone at `~/Desktop/syntax-repos/aat` on `candidemini-local`; that was moved to `~/Desktop/syntax-cowork-repos/aat` on 2026-09-21 and no longer exists at the old path.


## Two operating modes, and what changes because of them

A Claude session's access to this repo varies from one session to the next, and isn't something either side chooses in advance -- it's a property of how the session happens to be connected when it starts, and it can change mid-session too.

**Device-linked.** The session has a live bridge to Neel's own machine and can read and write files there directly, in place (via a sandboxed shell on the device, not by re-typing file contents from memory). This is the fast path: edits land exactly where they belong, there's nothing to reconcile, and Neel sees changes in his own working tree as they happen. When this bridge exists, use it -- don't manufacture the cloud-only workflow below out of habit when direct access is available. Note that the device's own shell is a separate, isolated Linux sandbox, not Neel's real macOS environment directly -- a `.venv` built with his native Python won't run there, so verifying by running the test suite from that shell means installing deps fresh into that sandbox's own Python (network permitting), not reusing his existing virtualenv.

**Cloud-only.** No bridge to Neel's machine exists (or one existed earlier in a session and dropped). In this mode Claude has only its own disposable cloud sandbox, with network access to clone the *public* `neelsmith/aat` GitHub repo but no push credentials and no way to reach Neel's filesystem at all. The only honest way to deliver work from this mode is:

1. Clone `https://github.com/neelsmith/aat.git` fresh into the sandbox.
2. Make the change there, verify it thoroughly (see "Verifying before delivering" below) -- this mode has no shortcut back to "just try it in the real checkout," so verification has to be conclusive on its own.
3. Produce a patch: `git add -N <new files>` to include untracked new files in the diff without staging or committing anything, then `git diff -- <paths>` to capture the change as a plain unified diff. Split large, mechanically-regenerated output (e.g. rebuilt `docs/`) into its own patch, separate from the hand-written change, so Neel can review each on its own terms.
4. Sanity-check the patch by applying it to a *second*, independent fresh clone and re-running the test suite there -- confirms the patch is self-contained and applies cleanly outside the sandbox it was authored in, not just in the checkout that produced it.
5. Deliver the patch file(s) as a download, with an explanation of what's in each and how to apply them (`git apply <file>` from the repo root), and say plainly that this session had no direct access to his machine.

A patch delivered into the conversation only reaches Neel's repo once *he* applies it -- it does not land in his working tree on its own, and there's no way to check that it did from cloud-only mode. If a later message in the same session gains device access after having delivered a patch this way, check first whether Neel already applied it (`git status`/`git diff` against the patch's own file list) before writing anything, rather than assuming the patch is still unapplied.

If device access becomes available *before* delivering anything -- even if it wasn't available earlier in the same session -- prefer writing directly and skip the patch dance entirely; a mid-session change back to device-linked mode is a reason to switch modes, not a reason to keep the cloud-only workflow going out of momentum.


## Standing constraints for AI-assisted changes

These hold in *either* mode, and don't expire when a session's conversation history gets compacted or a new session starts:

- Never touch anything under `quarto/` -- Neel's hand-written project website.
- Never touch `README.md` -- Neel hand-edits this himself.
- Never touch `releases.md` -- same, Neel's own.
- Never run `git commit` (or push) on Neel's behalf, under any circumstances. Every change -- whether it's a direct edit in device-linked mode or a patch in cloud-only mode -- is left for Neel to review and commit himself. This is exactly *why* cloud-only mode delivers patches rather than trying to find some other way to get a commit onto `main`.


## Verifying before delivering

Whichever mode produced a change, don't report it done until:

- The full `pytest` suite passes (see TESTING.md) -- not just the tests for the file touched, and run in an environment that actually has the project's dev dependencies installed (see the device-sandbox caveat above).
- Where the change produces output meant for some other real tool to consume -- a Mermaid diagram, a Graphviz digraph, an HTML page -- that output has actually been run through the real downstream tool at least once (e.g. Graphviz's own `dot` CLI, not just eyeballing the string), not merely unit-tested against assertions written by the same session that wrote the code.
- `docs/` regenerates cleanly via `utilities/build_docs.py` when the change touches anything with a public docstring, and (in cloud-only mode) that regeneration is delivered as its own patch, separate from the hand-written change, since it's large and mechanical.

  **`utilities/build_docs.py` still wipes the whole `docs/` directory before regenerating -- mention this before running it, don't run it silently.** `build()` does `shutil.rmtree(docs/)` on the assumption `docs/` holds only pdoc's own output, but `docs/` also holds Neel's separately-published, hand-rendered `quarto/` site (`docs/guides/`, `docs/using/`, `docs/reference/`, `docs/site_libs/`, `docs/about.html`, `docs/install.html`, and so on -- none of it reproducible by re-running either build system with their current configs: Quarto's own `_quarto.yml` renders to `quarto/quarto-build/`, not `docs/`, so that content gets into `docs/` only when Neel runs `quarto render` inside `quarto/` himself). Running `build_docs.py` deletes all of that before writing back only the four pdoc pages (`aat.html`, `aat/core.html`, `aat/english.html`, `index.html`, `search.js`) -- this happened for real on 2026-09-08 (see the session log below) and was only recoverable because the Quarto content was already committed, via `git checkout -- docs/`. Docs publishing is now entirely manual, on Neel's own schedule (the `.github/workflows/docs.yml` CI workflow that used to auto-run this on every push -- and would have deployed the same wipe to the live GitHub Pages site -- was removed on 2026-09-08 at Neel's request specifically because of this incident), so there's no automated recurrence risk anymore. The local collision is still real, though: if `build_docs.py` runs after Neel has `quarto render`-ed fresh Quarto output into `docs/`, that output is gone until he re-renders it. A session can still run `build_docs.py` when asked to, but should say plainly that it will wipe `docs/` first and ask Neel to re-run `quarto render` afterward if Quarto content was there, rather than assuming that step isn't needed.


## Session log

A short entry per session that changed something non-trivial or delivered work in cloud-only mode -- date, what happened, and where the result went. Not a substitute for git history; this is for the "why did I get a patch file" and "what was going on across sessions" questions git history doesn't answer on its own.

- **2026-09-07 -- Graphviz DOT rendering.** Started in cloud-only mode (no device bridge in the session's tool set at the time): cloned `neelsmith/aat` fresh, added `aat/core/graphviz.py` (`graph_to_dot()`/`save_dot()`, mirroring `graph_to_mermaid()`/`save_mermaid()`) and `aat/core/orientation.py` (orientation validation extracted out of `mermaid.py` so both renderers share it, same pattern as the earlier `coloring.py` extraction), plus tests and a new USAGE.md section. Verified the actual DOT output with the real `dot` CLI (`dot -Tsvg`), not just string assertions. Delivered as patches at first, since no device access was available -- but it turned out an *earlier* session (before this one's conversation history was compacted) had already written the core module changes (`graphviz.py`, `orientation.py`, the `mermaid.py`/`__init__.py` edits) directly to Neel's own checkout via a device bridge that was live at the time, so those patches duplicated work already sitting there uncommitted. Once device access came back mid-session, the remaining gap -- tests, the USAGE.md section, and this file -- was written directly to Neel's checkout instead, and the full suite was run there (`pip install -e ".[dev]"` into the device sandbox's own Python, not Neel's native `.venv` -- see the device-sandbox caveat above) rather than delivered as another patch. Lesson folded into the workflow above: check `git status` for a device-linked repo before assuming a prior patch is unapplied, and prefer direct writes the moment device access exists.
- **2026-09-08 -- `aat_main.py` writes a serialized analysis to stdout.** Device-linked the whole time (no cloud-only detour this session). Changed `aat_main.py` to write `serialize_analysis()`'s exact `#!passages`/`#!aatnodes` text to stdout instead of the old human-readable token/node dump, so `python3 aat_main.py --passage "..." > analysis.txt` produces a file `read_analysis()` (or `aat_reader.py`) can reload directly. That required a small change to a second file, `aat/english/pipeline.py`: its `analyze_passages()` was printing referential-validation warnings straight to stdout, which would have corrupted the new serialized output whenever `validate()` found a problem -- moved those prints to stderr. Verified beyond unit tests: ran the actual analyze -> serialize -> stdout path with `DummyLM`, redirected real stdout to a file, and reloaded it with `read_analysis()` to confirm the round trip, plus confirmed stderr stays empty on the clean-analysis path and carries the warning (not stdout) on a deliberately broken one. Added `tests/test_aat_main.py` and one regression test in `tests/test_english_pipeline.py` for the stderr routing.
- **2026-09-08 -- `aat_to_dot.py`, a stdin-to-DOT CLI script.** Device-linked the whole time. Added `aat_to_dot.py`: reads a serialized analysis from stdin (the `#!aatnodes` format `aat_main.py`/`write_nodes()`/`write_analysis()` all produce -- a `#!passages` block alongside it, if present, is ignored) and writes a Graphviz DOT digraph to stdout via `graph_to_dot()`, with `--orientation`/`--no-color` flags mirroring that function's own parameters. No LM needed at all, so `python3 aat_main.py --passage "..." | python3 aat_to_dot.py > analysis.dot` chains the two scripts with no network access on the second half. `aat.core.read_graph()` needs a real file path, not stdin text directly, so the script bridges the two with a throwaway temp file it cleans up itself (`tests/test_aat_to_dot.py` has a regression test confirming no temp file survives a run) rather than changing `aat.core.serialization` to accept text directly -- kept the core module's API surface as-is for a need only this one script has. Verified beyond unit tests: piped a real `write_analysis()` output through the actual script (via subprocess, not by importing it, so argparse and `__main__` are actually exercised) and confirmed the DOT output renders with the real `dot` CLI (`dot -Tsvg`). Documented in USAGE.md's Graphviz section, with a forward link from the `aat_main.py` section above it.
- **2026-09-08 -- moved `optimize_gepa.py` into `utilities/`.** Device-linked the whole time. `git mv optimize_gepa.py utilities/optimize_gepa.py`, then fixed the two `sys.path.insert()` calls and `dspy.GEPA`'s `log_dir` (all previously `Path(__file__).parent`-relative, which pointed at the repo root only because the script used to live there) to use a `REPO_ROOT = Path(__file__).resolve().parent.parent` constant instead, mirroring `utilities/build_docs.py`'s own existing convention exactly. Updated every doc/docstring reference across the repo to the new `utilities/optimize_gepa.py` path (`OPTIMIZING.md`'s invocation lines, `USAGE.md`, `TESTING.md`, `DEVELOPMENT.md`, `tests/fixtures/gold_examples.py`, `aat/english/gepa_metric.py`, `aat/english/dspy_signatures.py`) -- one reference in `quarto/reference/testing.qmd` still says the old bare filename, left alone since `quarto/` is off-limits; flagged it to Neel instead. Also fixed an unrelated, pre-existing stale reference in `DEVELOPMENT.md` noticed along the way (`docs/build_docs.py`/`docs/site/`, from before build_docs.py's own earlier move to `utilities/` -- see the 2026-08 "Reorg" commits) while touching that same paragraph. Verified beyond the test suite: ran `python utilities/optimize_gepa.py --help` and `build_trainset()` for real from the repo root to confirm the import chain (`aat_main`, `fixtures.gold_examples`, `aat.core`, `aat.english`) and path resolution actually work post-move, not just that they compile.
- **2026-09-08 -- CEX corpus support (`aat/core/cex.py`, `aat_corpus.py`).** Device-linked the whole time. The request named an external, precise format (CEX, the CITE Exchange format) I didn't have reliable enough knowledge of to guess at -- rather than assume a delimiter or block convention, used WebSearch/WebFetch to read the actual spec (https://cite-architecture.github.io/citedx/CEX-spec-3.0.1/) before writing any code: confirmed blocks are `#!`-labelled, `#!ctsdata` is two columns (URN, then text) separated by a delimiter the file's own author chooses with **no in-file declaration** of what it is (`#` is the common convention, not a rule), blank lines are ignored, and `//`-prefixed lines are comments. Added `aat/core/cex.py` (`parse_cex_ctsdata()`/`read_cex_passages()`, returning `CitedPassage` -- kept in `aat.core` since reading a corpus format into passages is language-agnostic input handling, the same boundary reasoning as `aat.core.serialization`, not an `aat.english` concern) and `aat_corpus.py` (repo-root script, sibling of `aat_main.py`: reads a CEX file or stdin, runs every passage through `analyze_passages()`, writes one combined `serialize_analysis()` string to stdout -- the corpus-level counterpart to `aat_main.py`'s single-passage version, chainable into `aat_to_dot.py` the same way). One deliberate deviation from `aat.core.serialization`'s own strict column-count checking: CEX row-splitting uses the delimiter's *first* occurrence only (`str.partition`), not an exact 2-field split, because the citable text half is real prose this project doesn't control and can plausibly contain the delimiter again (especially the default `#`) -- documented in `cex.py`'s own docstring. Verified beyond unit tests: built a real CEX sample file, ran the actual script end-to-end via `runpy` with `DummyLM` patched in for `aat_main._configure_lm` (the same technique `test_aat_main.py`/`test_aat_corpus.py` avoid needing for unit tests, but useful here for a true script-level smoke test without real API credentials), round-tripped the output through `read_analysis()`, and piped it through `aat_to_dot.py` into the real `dot` CLI to confirm the whole toolchain (CEX corpus -> analysis -> DOT -> rendered SVG) works together, not just each piece in isolation.
- **2026-09-08 -- `docs/` incident: `utilities/build_docs.py` deleted Neel's committed Quarto site output, recovered, root cause still open.** While verifying the CEX corpus feature above, ran `python3 utilities/build_docs.py` as the same "regenerate API docs" step done safely three times earlier this same session -- this time `build()`'s `shutil.rmtree(docs/)` wiped roughly 36 files: `docs/about.html`, `docs/install.html`, all of `docs/guides/*.html`, `docs/reference/*.html`, `docs/using/*.html`, `docs/search.json`, and the whole `docs/site_libs/` asset tree. Those turned out to be Neel's own hand-written `quarto/` site, already rendered and committed into `docs/` by some step outside this repo's own build scripts -- `quarto/_quarto.yml` configures Quarto's own output as `quarto/quarto-build/`, not `docs/`, so `build_docs.py`'s assumption that `docs/` holds only pdoc's own output was already false and nothing in the repo said so. Caught it via `git status --short` showing dozens of deletions under `docs/` immediately after the run. A first recovery attempt (restoring files one at a time from a list) failed on a shell-quoting bug -- `sed`-inserted quote characters ended up inside the pathspec argument literally, so `git checkout` treated `"docs/site_libs/..."` (quotes included) as the path and reported no match. Recovered cleanly instead with a single `git checkout -- docs/`, restoring the whole directory (both the deletions and this session's own regenerated pdoc pages) from HEAD in one step -- verified with `git status --short docs/` (clean) and directory listings confirming every Quarto file was back. Nothing was lost since the Quarto content was already committed. The root cause is NOT fixed and is now flagged in the "Verifying before delivering" section above with a standing "don't run `build_docs.py` without confirming with Neel first" warning: `.github/workflows/docs.yml` runs this same script on every push to `main` and deploys its output as the live GitHub Pages site, so the *next* push will reproduce this exact deletion on the live site unless Neel decides how the two build systems (pdoc and Quarto) should stop sharing one output directory -- separate subdirectories, separate deploy targets, or something else. That decision is his to make; nothing was changed in `build_docs.py`, `_quarto.yml`, or the CI workflow to work around it.
- **2026-09-08 -- removed the `docs.yml` CI workflow; docs publishing is now fully manual.** Direct follow-up to the `docs/` incident logged just above. Neel's decision: rather than reconcile pdoc and Quarto's shared `docs/` output directory, drop the automation entirely -- `git rm .github/workflows/docs.yml` (the workflow that ran `build_docs.py` and deployed `docs/` to GitHub Pages on every push to `main`; `.github/workflows/tests.yml`, the unrelated pytest-on-push workflow, is untouched). Neel will run `utilities/build_docs.py` and, separately, `quarto render` inside `quarto/` by hand whenever he wants to refresh either half of the site, then commit and push `docs/` himself. Updated `utilities/build_docs.py`'s own docstring (it referenced the now-deleted workflow by name), `notes/DEVELOPMENT.md`'s "Suggested cadence" section (previously said docs "rebuild automatically... no manual step needed"), and the `docs/`-incident warning in "Verifying before delivering" above -- the warning no longer tells a session to hold off running `build_docs.py` pending Neel's confirmation (the live-site CI risk that justified that gate is gone), but still says to flag the `shutil.rmtree(docs/)` behavior before running it, since it still silently destroys whatever Quarto has rendered into `docs/` since the last `quarto render`. Did not touch `quarto/_quarto.yml`, `quarto/`, or anything else Neel called off-limits.
- **2026-09-21 -- Cowork multi-machine setup.** Device-linked to `candidemini-local` the whole session. Neel asked for a Cowork project so he could develop `aat` across multiple machines. Cloned `neelsmith/aat` fresh to `~/Desktop/syntax-repos/aat`, then, at Neel's request, moved the whole checkout to `~/Desktop/syntax-cowork-repos/aat` (the naming convention he wants for Cowork-linked clones going forward). Added a root `CLAUDE.md` that just points to this file, so a session that only auto-reads `CLAUDE.md` still finds the real conventions here. While the move was happening, discovered Neel had been working directly in a terminal on the same checkout in parallel -- he'd checked out `dev`, made a "light edits" commit (README.md, an image move, quarto/index.qmd -- all his own files per the write restrictions above), and committed the `CLAUDE.md` this session had just written. The move picked up his latest state correctly; nothing was lost, but it's a reminder that Neel may be editing a device-linked checkout in his own terminal at the same time a session is working in it. Neel separately set up a second, independent clone of the same repo on another laptop (not Cowork-linked), at the same relative path `~/Desktop/syntax-cowork-repos/aat`, synced with `candidemini-local` by hand (`git pull`/`git push`, no automation) -- documented in the new "Local checkouts across machines" section above. Also walked Neel through how Cowork's device linking actually works when he asked how to get a session to act on the laptop instead: a Cowork task links to exactly one computer at a time (set when the task starts, or changed via "Link to this computer" in the desktop app), so the working recommendation was a *separate* Cowork task linked to the laptop rather than relinking this one back and forth -- which is exactly why this file's role as the continuity layer between otherwise-unconnected sessions matters here.
- **2026-09-21 -- linked `pangloss-m3-504-local` (Neel's other laptop) to Cowork.** Device-linked session started on this machine for the first time, at Neel's request specifically to get this machine set up with the same continuity workflow as `candidemini-local`. Folder connected at `~/Desktop/syntax-cowork-repos/aat`, an independent clone from `candidemini-local`'s (not synced automatically -- keep both in step by hand with `git pull`/`git push`). Repo was already clean and up to date with `origin/dev` (`nothing to commit, working tree clean`) when this session started. Read this file per its own instructions, updated the "Local checkouts across machines" section above to record this machine as linked, and added this entry. No project work done yet this session -- Neel asked for the linking and session-log continuity to be confirmed working first. Everything here is left uncommitted per the standing constraint above ("leave commit/push to me"); Neel will commit and push when ready.
- **2026-09-21 -- added `notes/VENV_SETUP.md`.** Device-linked on `pangloss-m3-504-local`, direct follow-up to linking this machine earlier the same day. Neel asked for a short note on setting up a venv synced to the project after a clean clone. Checked `pyproject.toml` (requires-python `>=3.10`, `dev` extra pulling in pytest/dspy/python-dotenv/pdoc/marimo), `.github/workflows/tests.yml` (canonical install step: `pip install -e ".[dev]"`, tested against 3.10-3.12), `.env.example`, and `.gitignore` (confirms `.venv/`, `venv/`, `.env`, and `uv.lock` are all local-only, never committed) before writing anything, so the note reflects the project's actual setup rather than a generic template. `notes/DEVELOPMENT.md` and `notes/TESTING.md` don't cover install/venv setup at all, so this doesn't duplicate anything there. Covers: creating and activating a `.venv`, the editable install with the `dev` extra (plus narrower `english`-only / core-only alternatives), copying `.env.example` to `.env`, and verifying with `pytest`. Notes that a venv isn't portable between machines or clones -- relevant since Neel now has multiple local checkouts (see "Local checkouts across machines" above) -- and that `uv` is a drop-in alternative. Left uncommitted per the standing constraint above.
- **2026-09-22 -- diagnosed a 405 from the SuarezAI litellm proxy; confirmed server-side, not fixable from this repo.** Device-linked on `pangloss-m3-504-local`. Neel reported `marimo/aat_graph.py` failing on any LM call (`analyze_passage()` -> dspy -> litellm -> openai client) with `litellm.exceptions.APIError: ... Error code: 405 - {'detail': 'Method Not Allowed'}` against `API_BASE=https://suarezai.holycross.edu/litellm`. Ruled out several things in order: (1) the two file edits made on this machine earlier the same day (`scratch/eng-rv-vpl-genesis.cex`, `notes/VENV_SETUP.md`/`CLAUDE_WORKFLOW.md`) -- neither touches any file in the failing code path, confirmed by inspection and `git status`/`git log`. (2) `.env`/`_configure_lm()` misconfiguration -- `API_BASE`/`MODEL` match `.env.example` exactly, `API_KEY` present and non-placeholder. (3) Client-library version drift -- this machine's `.venv` was built fresh the day before (following the new `notes/VENV_SETUP.md`) and pulled newer `litellm`/`dspy` than `candidemini-local`'s working checkout (`litellm` 1.102.0 vs 1.97.0, `dspy` 3.3.1 vs 3.3.0); checked `dspy 3.3.1`'s own dist-info (`Requires-Dist: litellm>=1.65.8`, no upper bound, so no conflict) and had Neel downgrade `litellm` to 1.97.0 to match -- **the 405 persisted**, ruling this out. (4) Confirmed server-side: a plain `curl -X POST` straight to `https://suarezai.holycross.edu/litellm/chat/completions` (bypassing dspy/litellm/openai entirely) reproduced the same 405. This device-linked shell's own network is locked down by an unrelated allowlist proxy (confirmed even `example.com` is unreachable from it), so all of the curl testing had to happen in Neel's own Terminal, not from a Claude session. Checked for a redirect next, with `curl -v` (no `-L`) on the exact request `_configure_lm()` sends: no redirect at all -- the very first and only response is `HTTP/2 405` immediately, `content-type: application/json`, body `{"detail":"Method Not Allowed"}`, `server: uvicorn`, and (the most telling header) `x-process-time: 0.000903` -- under a millisecond, too fast for a real upstream Anthropic call, meaning the request never reached litellm's actual completion-handling logic at all. That JSON body is Starlette/FastAPI's own default 405 payload (returned when a path is registered but not for the method used), which pointed at routing rather than an outage. Tried to confirm via the route table directly -- `curl`ing `/litellm/openapi.json`, `/litellm/docs`, and bare `/litellm` (all GET) -- but all three came back as a styled HTML 404 page, not Starlette's plain JSON 404 and not the app's own docs/schema. That asymmetry (a real, fast, uvicorn-flavored 405 on `POST /litellm/chat/completions` specifically, vs. a differently-styled 404 on every GET path tried, including ones that would normally be free with any FastAPI app) suggests this sits behind a gateway/ALB with explicit per-method route rules rather than a plain reverse-proxied FastAPI app -- consistent with the `AWSALB`/`AWSALBCORS` cookies also seen in the response. Net conclusion: `API_BASE=https://suarezai.holycross.edu/litellm` (as documented in `.env.example` and used by `_configure_lm()`) is either stale or the proxy's routing was reconfigured since it was last documented working -- this is not an outage (the service answers fast and normally) and not fixable from this repo. Handed off to Neel to raise with whoever administers the SuarezAI proxy, with this full curl trace as evidence, and to ask specifically what the current correct base path/method for chat completions is -- once that's confirmed, `.env.example`'s `API_BASE` comment and this file should both get updated to match. No repo changes made; this is an institutional service issue, not an `aat` bug.
- **2026-09-22 -- sentence-spanning corpus analysis, plus an LM-cost checkbox on every notebook that calls an LM.** Device-linked on `pangloss-m3-504-local`, same session as the 405 diagnosis above (Neel resolved that independently -- "OK, I'm once again able to connect to my Opus 5 instance. Back to work!" -- before this work started). Two requests bundled together: (1) `aat_corpus.py`'s per-citation-unit analysis is wrong whenever a sentence's grammar crosses a citation-unit boundary -- Neel's own example, real in `scratch/eng-rv-vpl-genesis.cex`: Genesis 1:14 ends mid-clause with `:`, completing only in 1:15. (2) match a UI pattern already established in Neel's other project, `arsgrammatica`'s marimo notebooks: a checkbox showing the cumulative dollar cost of all LM calls made so far in the session, on *every* notebook that calls an LM, not just the new one -- Neel confirmed this explicitly when asked to scope it ("It's in all the marimo notebooks that use dspy").

  Sentence-spanning analysis: added `aat/english/sentences.py` -- `passage_component()`/`urn_prefix()` split a CTS URN into its passage component (5th `:`-field) vs. everything before it; `ends_sentence()` checks for `.`/`?`/`!` by default (deliberately excludes `:`/`;`, chosen after checking the actual distribution of unit-ending characters in the scratch corpus -- those two introduce continuations, exactly the Genesis 1:14 case); `cluster_sentences()` groups an ordered list of citation units into the smallest sentence-ending runs; `tokenize_units()` tokenizes one such group as a single combined passage, giving every token an id unique across the whole group (`"{unit_passage}.{local_id}"`, e.g. `"1.14.t3"`, per Neel's own spec -- using the codebase's real `t1`-indexed convention rather than the `t0` he wrote, which doesn't match `tokenize()`'s actual output) and a combined context that's a CTS range reference for a multi-unit group (`...:1.14-1.15`) or a plain passage reference for a one-unit group; `tokenize_corpus_by_sentence()` chains both over a whole corpus. All of this lives in `aat.english`, not `aat.core`, since it's English-sentence-boundary-specific (would need to change for a language with different sentence-final punctuation), and none of it touches dspy or makes an LM call -- pure and deterministic given the same ordered units. `aat/english/pipeline.py` gained `analyze_units_by_sentence(units)`, the LM-dependent counterpart: runs each sentence group through `analyze()`/`validate()` (warnings to stderr, matching `analyze_passages()`'s existing convention) and returns `(tokens, graph)` in the same shape `analyze_passages()` does. 25 new tests (`tests/test_english_sentences.py`, plus 3 DummyLM-backed additions to `tests/test_english_pipeline.py`), all passing; verified beyond unit tests against the real (corrected -- see below) `scratch/eng-rv-vpl-genesis.cex`: 20 citation units read, correctly grouped into 18 sentences with 1.14+1.15 and 1.17+1.18 each merging into one sentence, and a full LM-free round-trip (re-tokenizing from the saved original passages reproduces the exact same contexts/ids a live analysis run produced) confirmed exact-match.

  Cost checkbox: added `aat/lm_cost.py` -- `summarize_lm_cost(history)` walks a dspy `LM.history` list and returns an `LMCostSummary` (total cost, count of priced calls, count of uncosted/cache-hit calls where `cost` is `None`), `format_lm_cost()` renders it for display. Kept as a top-level sibling module, not under `aat.core` and not re-exported from `aat/__init__.py` (whose own docstring says it re-exports only `aat.core` so plain `import aat` never needs dspy) -- notebooks import it directly (`from aat.lm_cost import ...`). No dspy import in the module itself, so this placement doesn't actually need to matter for the dspy-free guarantee, but it keeps the re-export list's existing meaning intact rather than making an exception. 7 new tests (`tests/test_lm_cost.py`). Retrofitted into `marimo/aat_graph.py`: a `mo.ui.checkbox(label="*See cost*")`, a `cost_summary` cell keyed to `lm.history` via the `_ = graph` trigger idiom (a cell only re-runs when a variable it reads is *reassigned*, not just mutated -- `lm.history` mutates in place on every call, so the cell instead depends on `graph`, which *is* reassigned each time a new analysis completes), and a conditional `costdisplay` cell -- same structure `arsgrammatica`'s notebooks already use, reimplemented (not copied) after reading four of them for reference, at Neel's explicit direction not to edit anything in that folder. Also fixed a latent bug found while doing this: the `lm = configure_lm()` cell was missing its `return (lm,)` tuple, so no other cell could actually depend on `lm`.

  New notebook, `marimo/aat_corpus_graph.py`: modeled cell-by-cell on `aat_graph.py`. File picker for a 2-column CEX corpus, a delimiter input (default `#`), a number input capping how many citation units to read (a real safety valve, not a nicety -- every sentence group is its own billed LM call), a separate "Analyze corpus" run button (kept as its own button rather than folded into a form with the file picker, out of caution since that composition couldn't be interactively verified from this sandbox), the same orientation control, cost checkbox, and Mermaid/highlighted-text display as `aat_graph.py` (reusing `tokens_to_html()`/`graph_to_mermaid()` unchanged -- both already support multi-context token/graph lists, confirmed against `aat_reader.py`'s existing precedent), plus a save button. The save step calls `write_analysis()` with the *original* per-citation-unit `units` list, not the merged sentence text, so the saved file stays reloadable without an LM call later -- the same reasoning `aat.core.serialization`'s own docstring already gives for saving original passages rather than merged ones. Verified via `py_compile`, `marimo check` (clean across all three notebooks together), and a live smoke test.

  Known gap, left for a follow-up rather than fixed now: `marimo/aat_reader.py` doesn't yet know how to reload a file `aat_corpus_graph.py` saved -- it only re-tokenizes with plain `aat.english.tokenize()`, which is correct for an `aat_graph.py`/`aat_corpus.py` file but not a sentence-spanning one (it would need `tokenize_corpus_by_sentence()` instead, to re-pair tokens with a multi-unit graph the same way). Flagged in `aat_reader.py`'s own area, in the new `notes/USAGE.md` section, and here -- not fixed, given the size this task had already reached.

  Also found and fixed, opportunistically, while smoke-testing against the real scratch file: `scratch/eng-rv-vpl-genesis.cex` had a bogus `urn|text` line as its second line -- real CEX `#!ctsdata` blocks have no header row at all (confirmed against `aat/core/cex.py`'s own spec-compliant parsing), so this line was being read as a real (bad) data row, context `"urn"`, and crashed `urn_prefix()` with `ValueError: 'urn' doesn't look like a CTS URN` the first time any code tried to split it into CTS fields. Removed the one line (`sed -i '2{/^urn|text$/d}' scratch/eng-rv-vpl-genesis.cex`); the file's other 1533 data lines are unaffected. This predates this session's own earlier edits to that file (the `GEN {s}:{s}` -> URN and space -> `|` conversions) -- it wasn't introduced by either of those, just never triggered a crash until something tried to parse the passage component out of every line's URN.

  Docs: new `notes/USAGE.md` section "Analyzing a corpus by sentence, across citation-unit boundaries", a forward-pointer from the existing `aat_corpus.py` section, updated paragraphs for `aat_graph.py` (cost checkbox), the new `aat_corpus_graph.py`, and `aat_reader.py` (the known-gap caveat above). `notes/TESTING.md`'s "What's covered where" list updated for the three new/changed test files. Full verification pass (`pytest`, `marimo check` on all three notebooks) run in the disposable device-sandbox venv before calling this done. Everything here left uncommitted per the standing constraint above -- Neel will commit and push when ready.
- **2026-09-22 -- LM output-token-budget management, matching arsgrammatica's `token_budget.py`.** Same session, direct follow-up to the sentence-spanning-analysis entry just above. Neel reported a notebook failure -- a truncated LM response -- and pointed out something diagnostic: the error's own `max_tokens=None` should never have been possible. That turned out to have two separate causes, both traced by first reading `arsgrammatica/token_budget.py` and its tests in full (arsgrammatica is the sibling project Neel connected read-only earlier this session for the cost-checkbox feature) rather than guessing at a fix: (1) every `dspy.LM(...)` construction in this repo (`aat_main.py`, both marimo notebooks) left `max_tokens` unset, falling through to `dspy.LM.__init__`'s own default of `None` -- confirmed directly against the installed dspy (3.3.1)'s real signature rather than assumed. `dspy.LM._check_truncation()`'s own warning always reports THAT baseline, never a per-call override, so leaving it at `None` was always going to produce a misleading `max_tokens=None` in any truncation-related message regardless of what else got fixed. (2) there was no per-call budget management at all -- every `analyze()` call used whatever `max_tokens` its LM happened to be constructed with, with no estimate based on passage length and no retry when a call actually got cut off, so a long combined passage (most likely from `analyze_units_by_sentence()`, where several citation units can combine into one unusually long sentence group) had nothing to fall back on but a raw `AdapterParseError`.

  Fix, in two parts, matching arsgrammatica's own resolved design: `aat_main.py`'s and both marimo notebooks' `configure_lm()`/`_configure_lm()` now pass an explicit `max_tokens=DEFAULT_CEILING` baseline (imported from `aat.english`) instead of leaving it at `None`, with a comment explaining why this specific baseline exists even though it's overridden per call. New module `aat/english/token_budget.py` -- placed under `aat.english`, not `aat.core` (it needs dspy) and not as a top-level `aat/` sibling like `aat/lm_cost.py` (that module deliberately has no dspy dependency; this one does, by design, per aat's own core/english boundary) -- with `estimate_max_tokens()` (a calibratable linear fit over input token count, generous untuned fallback constants until calibrated), `get_calibration()`, and `analyze_with_retry()`, which `aat/english/pipeline.py`'s `analyze_passages()` and `analyze_units_by_sentence()` now call instead of `analyze()` directly.

  One deliberate, documented divergence from arsgrammatica's own version: arsgrammatica's `SentenceAnalysis` output covers every input token 1:1 (`tokengraph`), so "does the result cover every input token id" is a reliable, LM-independent primary truncation signal there. `AgentActionTarget`'s `nodes` output has no such contract -- a correctly-analyzed passage normally covers only some of its tokens, and can legitimately produce zero nodes -- so that per-token-coverage check doesn't translate. This module's aat version leans entirely on the two schema-agnostic signals instead: `_finish_reason_was_length()` (the LM/provider's own claim, best-effort since not every proxy forwards it faithfully) and, for a response cut off badly enough to fail to parse at all, `_looks_truncated()` (a bracket/brace-balance check on the raw response text -- catches a real truncation even when a proxy under-reports `finish_reason`, which is exactly what the SuarezAI proxy diagnosed earlier this session turned out to do). A parse failure neither signal flags as truncation is retried once at the same budget with dspy's cache bypassed, on the assumption it's a one-off malformed-output glitch rather than something a bigger budget would fix -- same reasoning arsgrammatica's own version documents.

  Also added `utilities/calibrate_max_tokens.py` (adapted from arsgrammatica's own calibration script), following this repo's OWN established convention over arsgrammatica's -- reusing `aat_main.py`'s `_configure_lm()` and `tests/fixtures/gold_examples.py`'s `GOLD_EXAMPLES` via the same `sys.path` injection `utilities/optimize_gepa.py` already uses, rather than duplicating a `_configure_lm()` per script the way arsgrammatica's own scripts do. Not run against a real LM this session (no live credentials reachable from this sandbox) -- `estimate_max_tokens()` runs on its deliberately-generous untuned fallback constants until Neel runs it himself.

  17 new tests (`tests/test_english_token_budget.py`, mirroring arsgrammatica's own `test_token_budget.py` coverage where the design carries over, and diverging where it doesn't -- see the module docstring note above), all passing; full suite now 191. Verified the actual retry mechanism end-to-end beyond the test suite too: a hand-built scenario simulating the SuarezAI-proxy failure mode (a genuinely truncated raw response, `finish_reason` NOT reporting it) confirmed `analyze_with_retry()` grows the budget (1036 -> 2072 tokens for a 5-token passage) and succeeds on retry, exactly the case that caused the original failure. `notes/USAGE.md` gained a new "Managing the LM's output token budget" section plus a cross-reference from `aat_corpus_graph.py`'s own paragraph; `notes/TESTING.md`'s "What's covered where" list updated. Everything here left uncommitted per the standing constraint above.

- **2026-09-22 -- replaced the `#!passages` serialization block with `#!tokens`.** Same session, direct follow-up to the token-budget entry above. Neel raised a real gap in the saved-analysis format: an `#!aatnodes` block identifies agent/action/target tokens by id (e.g. `t3`), but the file gave no way to resolve an id back to its place in the surrounding text -- every OTHER token in the passage (the ones that didn't become a node) simply wasn't in the file at all, only recoverable by re-running `aat.english.tokenize()` against the `#!passages` block's raw text. Talked through the tradeoffs before touching anything: re-tokenizing on reload had two real problems beyond just being indirect -- it silently depended on `tokenize()` staying byte-for-byte identical to whatever version produced the original analysis (nothing would catch a future drift, it would just silently mis-pair old files), and it had no way at all to represent `aat.english.tokenize_corpus_by_sentence()`'s composite sentence-spanning ids (`"1.14.t3"`), which is exactly why `aat_reader.py` couldn't reload a file `aat_corpus_graph.py` saved -- the gap flagged (not fixed) in the sentence-clustering entry earlier this session. Neel's call: drop `#!passages` entirely rather than keep both, and use `#!tokens` as the block name.

  `aat/core/serialization.py`: replaced `serialize_passages()`/`write_passages()`/`read_passages()` (a `context|text` block of raw passage text) with `serialize_tokens()`/`write_tokens()`/`read_tokens()` (a `context|id|value` block -- every `CitableToken` field, one row per token, in reading order, since row order is now the only thing that records reading order for this block, unlike `#!aatnodes` where row position is meaningless). `serialize_analysis()`/`write_analysis()`/`read_analysis()` take/return `List[CitableToken]` instead of `List[CitedPassage]` -- callers now pass the actual `tokens` an `analyze_*()` call already returned, not a passage to be re-tokenized later. Re-exported from `aat/core/__init__.py` and `aat/__init__.py` in place of the old names.

  Every producer updated to pass its own already-computed `tokens` straight through instead of reconstructing a passage: `aat_main.py` (`_write_serialized_analysis()` now takes `List[CitableToken]`; the `CitedPassage` it used to build just to serialize is gone), `aat_corpus.py` (its own previously-discarded `_tokens` return value now goes straight to `serialize_analysis()`), `marimo/aat_graph.py` (dropped its `passage_for_save` cell -- built a `CitedPassage` from the form purely to serialize -- entirely; `filename_base` now derives from `tokens[0].context` instead), `marimo/aat_corpus_graph.py` (the save cell now depends on `tokens`, the output `analyze_units_by_sentence()` already produces, instead of the original per-citation-unit `units`).

  The real payoff is `marimo/aat_reader.py`: its load cell used to re-tokenize every `#!passages` row with plain `tokenize()`, which is why it never worked for a sentence-spanning file (see above). Now it's just `tokens, graph = read_analysis(str(path))` -- one line, no tokenizer dependency, no knowledge of citation-unit clustering at all, works identically for a file any of the four scripts/notebooks in this project wrote. This closes the `aat_reader.py` known-gap entirely, rather than the follow-up it was flagged as needing.

  `notes/USAGE.md` updated throughout -- "Saving and loading a graph" (new block shape, `tokens` passed straight through), "Analyzing a corpus by sentence" (dropped the stale "save the ORIGINAL per-citation-unit passages" rationale, now moot), "Running an analysis from the command line" / `aat_corpus.py` / `aat_to_dot.py` sections (block name references), and the `aat_corpus_graph.py`/`aat_reader.py` paragraphs under "Interactive notebook" (the reload caveat is gone). `aat/core/serialization.py`'s own module docstring now records the `#!passages` design and why it was replaced, for anyone who finds an old reference to it. Left two things deliberately untouched and flagged instead, per this repo's own established convention for out-of-bounds files: `quarto/guides/saving-loading.qmd` still documents the old `#!passages` format and `docs/` (pdoc output) is now stale for this module -- both are Neel's own to refresh (quarto/ is off-limits per the standing constraint above; `docs/` only regenerates safely with his own `quarto render` step in the loop, per the existing `build_docs.py` incident note). Also: `scratch/gen_50_4-6_analysis.txt` (gitignored, already on disk) is a real file in the old `#!passages` format -- `read_analysis()` will now raise on it (`file has no '#!tokens' block`) rather than silently doing the wrong thing; left alone since it's Neel's own scratch output, but worth knowing before reaching for it.

  Updated `tests/test_core_serialization.py` (rewrote every passages-block test as a tokens-block test, plus a new explicit round-trip test for a composite sentence-spanning id), `tests/test_aat_main.py`, `tests/test_aat_corpus.py`, and `tests/test_aat_to_dot.py` (block-name/shape assertions). Full suite: 193 passing (up from 191 -- two genuinely new tests, not just renamed ones). Verified beyond the test suite: a real end-to-end run through both `analyze_passage()` and `analyze_units_by_sentence()` with DummyLM, writing and reloading each with the real `write_analysis()`/`read_analysis()`/`tokens_to_html()`/`graph_to_mermaid()` (not mocks) -- confirmed the `#!tokens` block is human-legible (the full sentence is right there, in order, by id) and that the sentence-spanning composite-id case round-trips with zero warnings and zero re-tokenization. `marimo check` clean on all three notebooks.

  Noticed in passing, not caused by this change: `aat/english/token_budget_calibration.json` now exists on disk (untracked, real data -- `"model": "litellm_proxy/anthropic/Claude Opus 5"`, 4 samples) -- Neel evidently ran `utilities/calibrate_max_tokens.py` himself against his real LM sometime this session, confirming that feature (from the entry above) works end-to-end in practice. Left it alone.

  Everything here left uncommitted per the standing constraint above.
